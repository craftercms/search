<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd
       http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- JSR-303/JSR-349 support will be detected
    on classpath and enabled automatically -->
    <mvc:annotation-driven/>
    <context:annotation-config/>

    <bean id="crafter.properties" class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer">
        <property name="locations">
            <list>
                <value>classpath:crafter/search/server-config.properties</value>
                <value>${crafter.search.extension.base}/server-config.properties</value>
            </list>
        </property>
        <property name="ignoreUnresolvablePlaceholders" value="true"/>
    </bean>
    


    <bean id="crafter.corsFilter" class="org.craftercms.commons.web.CORSFilter">
        <property name="disableCORS" value="${crafter.search.cors.disable}"/>
        <property name="allowOrigins" value="${crafter.search.cors.origins}"/>
        <property name="allowMethods" value="${crafter.search.cors.headers}"/>
        <property name="allowHeaders" value="${crafter.search.cors.methods}"/>
        <property name="allowCredentials" value="${crafter.search.cors.credentials}"/>
        <property name="maxAge" value="${crafter.search.cors.maxage}"/>
    </bean>

    <import resource="classpath:crafter/search/provider/services-context.xml"/>
    <import resource="${crafter.search.extension.base}/services-context.xml"/>

</beans>